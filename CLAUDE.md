# The Analytics (anciennement "Analyste") — Outil d'analyse d'actions

## Contexte
Application personnelle web construite avec Claude (dans le chat claude.ai) pour remplacer un carnet papier d'analyse d'actions, méthode value/quality façon Buffett/Lynch. Le projet a été développé progressivement sur une longue conversation, feature par feature, à la demande de l'utilisateur (Nolan). On bascule maintenant sur Claude Code pour continuer le développement avec un vrai suivi de fichiers/Git.

## Stack technique — volontairement minimaliste
- **Un seul fichier `index.html`** autonome : HTML + CSS + JS inline, aucun framework, aucun build step.
- **Aucune dépendance externe** sauf Google Fonts (Inter pour l'interface, Source Serif 4 pour les titres, IBM Plex Mono pour les chiffres et tickers) chargées via `<link>`.
- **Stockage** : `localStorage` uniquement (clé `carnet-analyse-data-v1`), pas de backend.
- **PWA** : `manifest.json` + `service-worker.js` + `icon-192.png`/`icon-512.png` pour l'installation en application (hors-ligne, icône bureau). Prévu pour être hébergé sur GitHub Pages.
- Ne PAS introduire de framework (React, etc.) ni de build tool sans demande explicite — c'est un choix assumé de simplicité.

## Modèle de données
```
state = { companies: [Company...], activeId: string|null }
Company = {
  id, name, createdAt, updatedAt,
  currency: '$' | '€',  // devise unique de toute l'analyse, choisie à la création (voir plus bas)
  fields: { identite: {...}, financier: {...}, croissance: {...}, valorisation: {...}, personnelle: {...} },
  customFields: { identite:[], financier:[], ... },  // champs ajoutés librement par l'utilisateur
  position: { active, quantite, prixAchat, montantInvesti }  // toujours dans `currency`, plus de devise par ligne
}
```
- 5 sections fixes définies dans `const SECTIONS = [...]`, chaque champ a un `type` qui pilote son rendu (`text`, `textarea`→contenteditable riche, `number`, `duo`, `growthDuo`, `sharesTrio`, `wallStreetTrio`, `checklist`, `stars`, `moat`, `ceoFondateur`, `pieChart`, et plusieurs types `computed-*`).
- **Toujours ajouter une migration dans `migrateCompany(c)`** quand on change la forme d'un champ existant (voir les nombreux exemples dans la fonction) — ne jamais faire perdre de données déjà saisies par l'utilisateur.
- Une section 6 "Synthèse globale" (`buildInsightsSection`) calcule des insights à partir des données déjà saisies (carte récapitulative `buildGlobalRecap`, radar, profil de risque, plus/moins-value, résumé rédigé) — ne pas y ajouter de nouveau champ éditable, elle est 100% dérivée. La carte récapitulative marine est le même composant `buildRecapCard` que l'aperçu illustratif de l'accueil (données fictives) : modifier l'un modifie l'autre.
- Barre latérale "Mes analyses" : aucune pastille de couleur (choix de l'utilisateur). Affiche par contre, à droite du nom, la note de conviction en petites étoiles (`starsRowHtml`, classe `.si-stars`), uniquement si une conviction est renseignée. Les étoiles sont des SVG symétriques (`STAR_SVG`) pour que la demi-étoile soit coupée pile au centre ; `starsRowHtml(n, extraCls, id)` est la fabrique commune utilisée à la fois par le champ Conviction et la barre latérale, ne pas dupliquer la boucle de 5 étoiles ailleurs.
- **Tri de "Mes analyses" (barre latérale)** : bouton à double flèche (haut/bas) à droite du titre, qui ouvre un petit menu (`#sortMenu`, `paintSortMenu`, `setSidebarSort`) : critère (`SORT_KEYS` : date de création, ordre alphabétique, note en étoiles, dernière modification) puis sens, avec des libellés adaptés au critère (A → Z / Z → A, plus récentes d'abord...). Choix mémorisé dans `state.settings.sortKey`/`sortDir` (défaut `created`/`asc` = ordre d'ajout, comme avant). Changer de critère remet son sens naturel (alphabétique et création croissants, note et modification décroissants). Le tri ne s'applique qu'à la liste de la barre latérale (`sortedForSidebar`, utilisée par `renderTabs`), jamais à `state.companies` lui-même. Les analyses sans note restent toujours en bas. `touchUpdated` enregistre aussi `c.updatedTs` (horodatage précis) car `updatedAt` n'a que la précision du jour.
- **Symbole de devise affiché automatiquement** : tout champ qui représente un montant reçoit le marqueur `money:true` dans sa définition `SECTIONS` (ou sur le `sub` concerné pour un `duo`, ex. `dateIPO.prixInitial`). `render()` affiche alors `c.currency` juste après le champ (`.value-suffix` pour le rendu générique et `number-ecart`, un `.value-suffix` aussi dans un `duo`) — aucune saisie du symbole n'est nécessaire. Champs marqués `money:true` : `marketCap`, `dateIPO.prixInitial`, `chiffreAffaires`, `beneficeNet`, `fcf`, `capex`, `totalActif`, `detteNette`, `detteTotal`, `epsActuel`, `epsEstime1an`, `valorisationDcf`, `valeurIntrinseque`, `prixAchatCible` (`prixActuel`, la position et les champs déjà calculés ont leur propre affichage de devise, voir plus bas). Les ratios et comptages sans unité (PEG, bêta, dette nette/FCF, PE, nombre d'actions...) n'ont pas ce marqueur.
- **Nb d'actions (`sharesTrio`) : "Disponible" est calculé, plus saisi** (demande de Nolan le 2026-09-24) : Free Float et Closely Held sont des nombres d'actions saisis (formats `9 Md`, `900M`, lus par `parseMoney`), et `computeDisponible(c)` donne Free Float ÷ (Free Float + Closely Held) en % (vide tant que les deux ne sont pas renseignés). Affiché en case calculée (`#computed-financier-nbActions-dispo`) avec une pastille NEUTRE bleue (`pill-watch`, jamais vert/rouge, car un flottant faible n'est pas "mauvais" en soi) : moins de 50 % "Capital très concentré", 50 à 80 % "Flottant modéré", plus de 80 % "Flottant large, titre liquide" (`disponibleLabel`, infobulle `tipDisponible`). Rafraîchi par `refreshAllComputed`. L'ancienne valeur saisie `nbActions.disponible` n'est plus lue ni affichée (inoffensive dans les données existantes).
- **Symbole % affiché automatiquement (même principe que `money:true`)** : marqueur `percent:true` sur le champ (ou le `sub` d'un `duo`, ex. `croissanceCA.subs.cagr`), qui ajoute un `<span class="value-suffix">%</span>` après le champ, y compris pour les champs à barème (`value-with-badge`, la pastille colorée vient après). Aucune saisie du signe % n'est nécessaire. Champs marqués `percent:true` : `margeBrute`, `margeNette`, `roe`, `roic`, `positionShort`, et les subs `cagr` de `croissanceCA`/`croissanceBenef`/`croissanceAttendueCA`/`croissanceEstimeeEps`. Ces quatre champs sont de type `growthDuo` (`croissanceEstimeeEps`, section Valorisation, 3 ans, est devenu `{ cagr, total }` le 2026-09-25 avec migration de l'ancien nombre simple vers `cagr` : le PEG ne change pas ; `computePeg` = PE ÷ le CAGR, JAMAIS le total) (depuis le 2026-09-25 : avant, le "Total" de `croissanceCA`/`croissanceBenef` était saisi à la main) : seul le CAGR est saisi (avec son `%` en `.value-suffix`), le "Total" est CALCULÉ `(1 + CAGR)^années − 1` (`growthTotalText`, années = `field.years`, 5 ans pour le CA et les bénéfices, 3 ans pour le CA attendu), affiché dans une case bleue suivie du même `%` en `.value-suffix`, mis à jour par `refreshAllComputed` pour tout champ `growthDuo`. Un ancien "Total" saisi reste dans les données mais n'est plus lu ni compté dans le remplissage (`isFieldFilled` ne regarde que `cagr` pour `growthDuo`). Tout nouveau champ monétaire ou en pourcentage doit recevoir `money:true`/`percent:true`.
- **Visibilité des symboles $ et %** : `.value-suffix` (utilisée pour toutes les unités : devise, %, bêta `β`...) est volontairement plus affirmée que les autres légendes de l'app : `color:var(--ink)` (texte plein contraste, pas `--ink-soft`) et `font-weight:700` à 15px, pour que le symbole se voie au premier coup d'œil à côté du chiffre au lieu de se fondre dans le fond (demande de Nolan le 2026-09-23, "les rendre plus visible"). Ne pas réutiliser `.value-suffix` pour autre chose qu'une unité de mesure, sous peine de la rendre disproportionnée.
- **Devise unique par analyse** (`c.currency`) : choisie dans la modale "Nouvelle analyse" (mémorisée dans `state.settings.defaultCurrency` pour la prochaine fois) et modifiable à tout moment via la bascule `$`/`€` en haut de la fiche (`#companyCurrencyToggle`, fonction `setCompanyCurrency`). Elle remplace les anciens sélecteurs de devise par champ (prix actuel, prix d'achat, montant investi) : tous les montants d'une même analyse s'affichent automatiquement dans `c.currency`, plus besoin d'écrire le symbole. Changer la devise ne convertit PAS les montants déjà saisis (juste leur étiquette et les calculs d'affichage) — c'est volontaire, il n'y a pas de taux de change dans l'app. `formatMoneyDisplay(n, currency)` et tous ses appelants doivent recevoir `c.currency` explicitement (pas de valeur par défaut cachée). `migrateCompany` valide strictement `c.currency` (seulement `'$'` ou `'€'`, tout le reste retombe sur `'$'` ou sur l'ancienne devise par champ si elle existe) et supprime les anciens champs `prixActuelDevise`/`prixAchatDevise`/`montantInvestiDevise`.

## Conventions établies (à respecter)
- **Langue** : tout en français.
- **Champs (depuis l'identité visuelle v2)** : label au-dessus de la valeur (14px, semi-gras, pastille "i" quand une infobulle existe), valeur dans un champ encadré (15px, Inter). Les valeurs calculées automatiquement gardent leur fond bleu clair (`--calc-bg`/`--calc-text`), jamais en gras. **Grille des sections (refonte de mise en page du 2026-09-24)** : `.section-body` est une grille à 12 colonnes ; un `.field-row` prend 4 colonnes par défaut (3 par ligne), et chaque champ de `SECTIONS` peut porter `span` (3, 4, 6 ou 12, posé en `data-span` par la boucle de rendu) pour aligner des indicateurs similaires côte à côte (ex. ROE/ROIC/marges, 4 par ligne) et `group` (sous-titre pleine largeur `.field-group`, teinté de `var(--sec-c)`, inséré quand le groupe change). Les champs larges (texte riche, camembert, liste, checklist, image, MOAT, répartition des actions) restent sur toute la largeur via `:has(:where(...))` dans `.field-row` (le `:where` annule la spécificité pour qu'un `data-span` puisse les remettre sur une demi-largeur, ex. deux textes côte à côte ; les deux camemberts "Revenu par source/pays" restent volontairement l'un sous l'autre, pleine largeur, faute de place côte à côte, demande de Nolan). L'ordre des champs dans `SECTIONS` peut être changé librement (les données sont indexées par clé). Sous 1180px : 2 colonnes, sous 640px : 1 colonne ; chaque nouveau champ doit recevoir un `span` et si possible un `group` cohérents pour ne pas laisser de trou dans la grille. Attention : tout `.field-row` posé dans une AUTRE grille (comme `.position-fields`, 3 colonnes) doit remettre `grid-column:auto`, sinon le `span 4` y crée des colonnes implicites. Dans Mon espace, `.sgrid` est aussi à 12 colonnes (`.s5`/`.s6`/`.s7`/`.s12`) : associer les cartes par paires (5+7, 6+6) pour éviter une carte seule sur une ligne.
- **Largeur des champs courts (Chiffres, %, ticker, dates courtes) vs champs de texte libre** : un champ dont la donnée est courte par nature est petit par défaut et s'agrandit avec la saisie (attribut HTML `size`, recalculé en direct par les listeners génériques `if(el.hasAttribute('size')) el.size = ...`), pour que le symbole de devise ou le badge de barème reste collé au chiffre. `fieldInput()` pose `size` dans quatre cas : `type:'number'`/`'number-ecart'` (plancher 2 caractères), `type:'ticker'` (plancher 3), `field.money` en texte libre comme `chiffreAffaires` (plancher 4), et `SCALE_CONFIG[field.key] || field.compact` pour un champ court non monétaire comme `margeBrute`/`ratioVenteADecouvert`/`dateCreation` (plancher 3, `field.compact` est un marqueur ad hoc pour un champ ponctuel sans autre raison d'être court). Pour un `duo` (ex. `dateIPO`, `croissanceCA`), `compactSub = field.autoWidth || sub.money || SCALE_CONFIG[field.key]` déclenche la même classe `.duo-item-auto` sur le sub concerné. Depuis le 2026-09-24, ces cases utilisent en plus `field-sizing:content` (bloc CSS juste avant `.value-with-note`, largeur exactement égale au texte, marge intérieure réduite à 11px, `min-width:38px`) : l'espace entre le dernier chiffre et le symbole $ ou % vaut 6px au total (`.value-suffix` `margin-left:6px`, `-6px` dans `.value-with-badge` qui a déjà un `gap:12px`). L'attribut `size` n'est plus qu'un repli pour les navigateurs sans `field-sizing`. Le filet CSS général `.field-value > input[size]{width:auto !important;min-width:56px}` (enfant DIRECT de `.field-value`, pour ne pas interférer avec les habillages `.value-with-suffix`/`.value-with-badge`/`.value-with-ecart` qui gèrent déjà leur propre largeur au même min-width) couvre tout champ court qui n'a pas d'habillage dédié. Un champ de texte libre (secteur, pays, siège social, notes) reste volontairement pleine largeur : ne pas lui ajouter `size`, `money`, ni `compact`.
- **Quand une infobulle s'affiche (demande de Nolan le 2026-09-23)** : sur un libellé de champ, uniquement au survol direct du petit "i" (pseudo-élément `.field-label[data-tooltip]:after`, repéré par la position du curseur dans `initCustomTooltips`/`overInfoIcon` ; le libellé est réduit à son contenu par `align-self:flex-start` pour que le "i" en occupe la fin), plus au survol des cases calculées automatiquement (`.computed`, `.ecart-slot`). Survoler le texte du libellé n'affiche rien. Les autres `data-tooltip` (pastilles de barème, boutons, en-têtes de tableau, bascule de devise, avatar) restent au survol direct de l'élément.
- **Écart cours / valeur estimée ("vs DCF", "vs Intrinsèque", `computeEcart`, corrigé le 2026-09-25 à la demande de Nolan)** : résultat signé, négatif = sous-évaluation, positif = surévaluation. Sous-évaluation = `(prix − valeur) ÷ valeur` (marge de sécurité, jamais plus de 100 %). Surévaluation = `(prix − valeur) ÷ prix` (de combien le cours devrait BAISSER pour rejoindre la valeur, toujours sous 100 %) ; avant, on divisait aussi par la valeur, ce qui donnait +100 % pour un cours du double de la valeur alors qu'une baisse de 50 % suffit. Valeur nulle ou négative : pas de calcul. Tous les appelants gardent la même convention de signe (seuils : marge de sécurité de Buffett quand `e < -0.05`, alerte Portefeuille quand `e > 10`, décote quand `e <= -15`) ; les textes qui parlaient d'un cours "au-dessus de la valeur de X %" disent maintenant que le cours "devrait baisser de X %". `tipEcart` affiche la formule du côté concerné. Les textes de la section Apprentissage (formule de la marge de sécurité, dictionnaire de `valeurIntrinseque`) donnent les deux formules.
- **Résumé de valorisation (`buildValuationSummaryBlock`, chips `.vs-chip`, revu les 2026-09-25 et 2026-09-26 à la demande de Nolan)** : 8 chips (2 lignes pleines de 4, ajout du PE forward pour combler la case vide), dans l'ordre : PE (actuel, sans couleur), PE forward, PEG, PE / Secteur, vs Intrinsèque, Wall St. moy (% calculé), Consensus (titre anglais de la jauge, dans sa couleur, petite police `.vs-value-sm`), Position short (`%`, couleur du barème `positionShort`). MOAT et "vs DCF" ont été retirés : le DCF dépend trop des hypothèses, la valeur intrinsèque est la référence partout dans l'app (marge de sécurité, alertes, Mon espace) ; les deux champs de saisie DCF et Valeur intrinsèque, eux, restent. Grille de 4 colonnes (3 puis 2 en dessous de 1180 et 640 px). Rafraîchi par `refreshValuationSummary` (appelée par `refreshDerived`, par la saisie des cases Wall Street et par `setWsReco` au relâchement de l'aiguille).
- **Wall Street, disposition (2026-09-25)** : `.ws-top` = grille de deux colonnes (sous 1180 px : une seule) : à droite la carte "Prévision long terme" (`buildWsLongTerm`, `wsLtIndicatorsHtml`, `computeLongTerm`, `#wsLongTerm` ; remplace la carte "Potentiel à 1 an" jugée peu utile par Nolan le 2026-09-26). Deux cases de saisie, `wallStreet.ca5` (chiffre d'affaires annuel estimé dans 5 ans) et `wallStreet.benef5` (bénéfice net annuel estimé dans 5 ans), acceptent les unités (`700 Md`, `190B`, comptent dans le remplissage), puis des indicateurs CALCULÉS avec les chiffres actuels de la section Financier (`chiffreAffaires`, `beneficeNet`, `margeNette` en repli) et le Market Cap de la section IDENTITÉ (`identite.marketCap`, repli sur `financier.marketCap`) et le prix actuel : CAGR implicite du CA et du bénéfice net sur 5 ans `(estimé ÷ actuel)^(1/5) − 1`, colorés avec les barèmes `croissanceCA` et `croissanceBenef` ; marge nette dans 5 ans (`bénéfice ÷ CA`, barème `margeNette`, comparée à la marge actuelle en points) ; PE sur le bénéfice futur (`capitalisation ÷ bénéfice estimé` ; SANS capitalisation renseignée, repli sur `PE actuel × bénéfice actuel ÷ bénéfice estimé`, champ `pe5Src` 'cap' ou 'pe' ; comparé au PE actuel) ; cours théorique dans 5 ans à PE constant (`cours actuel × bénéfice estimé ÷ bénéfice actuel`, sans dividende ni variation du nombre d'actions, note affichée dans la carte). Un bénéfice actuel négatif ou nul rend le CAGR du bénéfice non calculable (message explicite). Seule la partie `#wsLtInd` est refaite pendant la frappe (`refreshWsPercents`), jamais les champs de saisie, sinon le focus serait perdu ; `compactMoney` formate 391 Md$, 2,85 T$. Sous 1180 px la carte passe sous les autres. et à gauche (`.ws-left`, 480 px maximum, le surplus de hauteur est réparti entre ses deux cartes pour aligner le bas) deux cartes de même largeur l'une sous l'autre : `.ws-cols` (UNE carte, trois colonnes `.ws-col` séparées par un filet fin, libellés écrits en entier « Maximum », « Moyenne », « Minimum » ; le prix cible en haut et, DESSOUS, le pourcentage calculé et sa pastille, pour bien distinguer les trois ; compacte, ~150 px de haut, sous 640 px les colonnes s'empilent) puis la carte de la jauge du consensus (texte à gauche, jauge de 200 px à droite, ~140 px de haut). Un premier essai avec les cartes à gauche et la jauge à droite a été rejeté par Nolan (cases trop grandes et pas belles). Le graphique en éventail vient ensuite, pleine largeur (ses étiquettes internes restent « Max / Moy / Min »). Il n'y a PLUS de bouton "Effacer" sur la jauge (retiré à la demande de Nolan) : on l'efface au clavier (Suppr ou Retour arrière quand elle a le focus).
- **Barème couleur** : système à 4 paliers (tresbon/bon/moyen/mauvais) défini dans `SCALE_CONFIG`, gommettes colorées (`TIER_COLORS`), infobulles personnalisées (pas de `title` HTML natif — système `data-tooltip` + `initCustomTooltips()` avec rendu riche coloré).
- **Champs monétaires** : parser `parseMoney()` qui gère les suffixes K/M/Md/T, plus B et Bn = milliard comme Md, Mds et Mrd aussi, les espaces de milliers (`3 200 Md`) et le symbole € (avant, `2,85 T€` ou `3 200 Md` perdaient silencieusement leur unité et valaient 2,85 ou 3) (usage des sites américains, demande de Nolan le 2026-09-25 ; `391B` = `391Md`) et le symbole $/€, et la virgule décimale française (`num()` gère aussi la virgule).
- **Couleurs de la NOTE GLOBALE (2026-09-26, demande de Nolan)** : 5 paliers de 20 points, rouge (moins de 20), orange (20 à 40), jaune (40 à 60), vert clair (60 à 80), vert foncé (80 et plus) : `scoreTier5`, `scoreColor5(note, onDark)` (teintes du thème clair/sombre, ou teintes vives `TOOLTIP_TIER_COLORS` + orange `#fb923c` sur les cartes à fond marine), `scoreScaleTooltip` (légende dans l'infobulle de l'anneau de l'en-tête et de la carte récapitulative). Utilisé partout où la note globale est colorée : anneau de l'en-tête, carte récapitulative, polygone du radar, Score moyen et classement de Mon espace, tableau de toutes les analyses. Ce barème à 5 paliers ne concerne QUE la note globale : les indicateurs et les axes du radar gardent le barème à 4 paliers (`TIER_COLORS`), et la checklist Buffett aussi (`scaleTier(.., [80, 60, 40])`). Orange thème clair/sombre `#f2994a` (le même que la sécurité du dividende).
- **Solidité et solvabilité (2026-09-26, demande de Nolan)** : (1) CORRECTION de `computeDetteActif` : un ratio négatif était toujours noté très bon, même avec un FCF négatif et de la dette ; maintenant dette nette ≤ 0 donne un ratio ≤ 0 (très bon), dette > 0 avec FCF ≤ 0 renvoie `DETTE_NON_REMBOURSABLE` (99, noté mauvais, affiché « n.s. » par `fmtDetteActif`, jamais afficher directement `dette.toFixed`). (2) Deux cases calculées avec pastille dans Financier, Bilan et endettement : `detteTotalActif` (dette totale ÷ total actif en %, barème inversé 20/35/50) et `tresorerieDette` (trésorerie ÷ dette totale en %, barème 100/50/25, non calculable sans dette). (3) Groupe « Solvabilité et risque de faillite » : DEUX cases de saisie simples (première version en Z-Score d'Altman calculé avec 4 chiffres du bilan, abandonnée le 2026-09-26 car trop lourde à renseigner ; ces données se trouvent toutes faites sur les sites financiers) : ``scoreSolvabilite`` (note de 0 à 100, suffixe `/100`, barème 75 / 50 / 25 : très solide, solide, fragile, très fragile) et ``probaFaillite`` (probabilité de faillite en %, barème inversé 1 / 5 / 20 avec inclusive:true : 1 % ET MOINS très bon, jusqu'à 5 % bon, jusqu'à 20 % moyen, plus de 20 % mauvais ; l'option inclusive de SCALE_CONFIG fait appartenir la borne au meilleur palier, scaleTierInverse(val, t, inclusive), et l'infobulle affiche « ≤ 1 % »), chacune avec pastille de couleur et infobulle de barème (branche générique ``SCALE_CONFIG``, qui accepte maintenant aussi ``field.suffix`` en plus de ``percent``). Ce sont des estimations d'un modèle statistique relevées par l'utilisateur : le dictionnaire précise que ce n'est jamais une prévision. Les anciens champs ``totalPassif``, ``fondsRoulement``, ``beneficesNonDistribues``, ``resultatExploitation`` n'existent plus (des valeurs éventuellement saisies restent dans les données, ignorées). (4) Les six indicateurs (dette nette / FCF, conversion du FCF, dette totale / actif, trésorerie / dette, score de solvabilité, probabilité de faillite) alimentent l'axe Solidité du score. Ajouter ces champs baisse un peu le pourcentage de remplissage des fiches existantes. 
- **Score composite (refondu le 2026-09-26 à la demande de Nolan)** : `computeRadarAxes` renvoie 5 axes `{ id, label, weight, score, tier, parts }` (définis par `SCORE_AXES`). Chaque axe est la MOYENNE des notes 0 à 100 (`continuousScore` avec les seuils de `SCALE_CONFIG`, une note de 75 correspond au seuil du palier très bon) de ses indicateurs renseignés : Qualité 25 % = MOAT (notation LINÉAIRE : 1 point = 10, donc 7/10 = 70, demande de Nolan le 2026-09-26 ; la pastille du champ MOAT garde son barème 8 / 6 / 4) + marge brute + marge nette + checklist Buffett (notation LINÉAIRE : chaque critère validé vaut 10 points sur 10, donc 7/10 = 70 points, demande de Nolan ; les couleurs de la checklist ailleurs gardent 80 / 60 / 40 ; IGNORÉE tant qu'aucun critère n'est validé, pour ne pas compter à 0 une checklist jamais commencée ; attention, plusieurs critères se cochent seuls à partir d'autres indicateurs, donc un léger double comptage assumé, demande de Nolan le 2026-09-26) ; Rentabilité 25 % = ROIC + ROE + CAGR du bénéfice net ; Croissance 15 % = CAGR du CA + croissance attendue du CA sur 3 ans + croissance estimée du BPA (EPS) sur 3 ans (chaque axe compte au moins 3 indicateurs depuis le 2026-09-26 ; croissanceEstimeeEps a un barème 20 / 10 / 0 et une pastille dans son champ) ; Solidité 20 % = dette nette / FCF + conversion du FCF + dette totale / total actif + trésorerie / dette totale + score de solvabilité + probabilité de faillite ; Valorisation 15 % = PEG + marge de sécurité + PE / secteur + consensus des analystes (jauge `wallStreet.reco` de 1 à 5, note LINÉAIRE `(reco − 1) ÷ 4 × 100` : Strong Sell 0, Sell 25, Hold 50, Buy 75, Strong Buy 100 ; ignoré tant que la jauge n'est pas réglée ; retenu plutôt que la position short, plutôt un indicateur de risque et de sentiment que de valorisation, à la demande de Nolan le 2026-09-26). La marge de sécurité est la remise du cours sur la valeur intrinsèque, `−computeEcart` en %, avec sa propre échelle `MARGE_SECURITE_SCALE` (30 % et plus très bon, 15 à 30 bon, 0 à 15 moyen, cours au-dessus de la valeur mauvais ; elle n'est pas dans `SCALE_CONFIG`). La note globale (`computeGlobalScore`) est la moyenne pondérée des axes renseignés (poids réajustés). FIABILITÉ : `computeScoreInfo(c)` donne `{ score, filled, total, provisional, axes }` ; la note est `provisional` tant que moins de `SCORE_MIN_AXES` = 3 axes sur 5 sont renseignés (une fiche avec le seul MOAT à 10 donnait 100/100 avant) : mention « provisoire » sur l'anneau de score de l'en-tête (avec « n/5 axes »), sur la carte récapitulative (`scoreNote`), dans le classement de Mon espace et note « basée sur n axes sur 5 » sous la légende du radar ; le survol d'un axe du radar (`axisTooltip`) détaille ses indicateurs. Rendre un axe utilisable = ajouter la clé dans `SCORE_AXES`, `SCORE_PART_LABELS`, `SCORE_PART_UNITS` et `scorePartValue`. Toute modification change TOUTES les notes existantes (recalculées à l'affichage, aucune migration).
- **Mise en forme riche** : les zones de texte libre sont des `contenteditable` (pas des `<textarea>`) pour permettre gras/souligné via la barre d'outils flottante (`initFormatToolbar`).
- **Texte collé = texte brut** : `initPlainTextPaste()` intercepte le collage et le glisser-déposer externe dans `.rich-textarea` et n'insère que le texte brut (les sauts de ligne sont conservés), pour que tout texte ait le même formatage que le reste de l'app. `sanitizeRichHtml()` ne conserve que `b`, `strong`, `u`, `br`, `div` (sans attribut) et nettoie les anciennes données une fois (drapeau `_richSanitized` dans `migrateCompany`). Un filet CSS (`.rich-textarea *`) neutralise tout style en ligne restant. Toute nouvelle zone de texte riche doit être ajoutée à `RICH_FIELDS`. `newCompany()` pose `_richMigrated` et `_richSanitized` à `true` : ne pas retirer ces drapeaux, sinon les textes en gras d'une nouvelle analyse sont échappés au rechargement.

## Identité visuelle v2 (application professionnelle)
- **Coque** : barre latérale marine fixe à gauche (marque, Accueil, liste des analyses, bouton "Nouvelle analyse", bascule clair/sombre) + zone de contenu. Sur mobile (< 900px) la barre latérale devient un tiroir ouvert par le bouton menu. Le rendu de la liste est fait par `renderTabs()` (nom conservé) dans `#tabs`.
- **Thème** : `data-theme="light|dark"` posé sur `<html>` par un petit script dans le `<head>` (préférence système par défaut, choix mémorisé dans `localStorage` clé `analyste-theme`). Toutes les couleurs passent par des variables CSS (`:root` et `:root[data-theme="dark"]`). Les couleurs du barème (`TIER_COLORS`) sont ajustées par `applyTierPalette()` en thème sombre.
- **Thème clair (retouché)** : sur demande explicite, le mode sombre ne doit plus être touché ("il est top"). Le mode clair a été assoupli (`--bg`/`--paper:#edf0f5`, `--card`/`--field:#f9fafc`, au lieu d'un blanc pur qui "pique les yeux") et son bleu marine (`--navy`/`--navy-2`/`--navy-3`, utilisé par la barre latérale, la bande d'accueil, `.recap`, les avatars...) a été éclairci (`#17294e` et non plus `#0a1224`) pour que le bleu marine change vraiment visuellement entre les deux thèmes, alors qu'avant il restait presque identique (quasi noir) dans les deux. Ne jamais modifier les valeurs du bloc `:root[data-theme="dark"]` sans demande explicite.
- **Typographie** : texte courant 14px, valeurs saisies 15px, légendes jamais sous 12-13px. Variables `--fs-caption`, `--fs-body`, `--fs-value`.
- **Accueil** (`renderHomeView`) : bandeau d'accroche + aperçu illustratif (`buildHeroPreview`, données fictives), bande de chiffres calculés depuis `SECTIONS`/`SCALE_CONFIG`/`BUFFETT_RULES`, tableau de bord des analyses (`buildHomeDashboard`) puis arguments de vente, encart pédagogique, étapes, appel à l'action et avertissement (pas un conseil en investissement). Les textes marketing sont au vouvoiement et ne doivent contenir aucun chiffre inventé. **Visiteur** (`visitor = !cloud.session && !state.companies.length`, depuis le 2026-09-25) : le bandeau propose "Créer un compte gratuit" (`data-auth-open="signup"`) et "Essayer sans compte" (`data-new-analysis`), plus une ligne "Déjà un compte ? Se connecter", et une section `#account` compare "Sans compte" et "Avec un compte gratuit" ; le bandeau final reprend les deux boutons. Les textes de l'accueil ne doivent plus dire "aucun compte" ni "100 % local" (c'est faux depuis les comptes en ligne) : dire "compte facultatif". Les dépendances de l'accueil au compte sont re-rendues à la connexion, déconnexion et suppression de compte.
- **Modèles d'e-mails Supabase** : dossier `modeles-emails/` (ignoré par Git), à coller à la main dans Supabase, Authentication, Emails, Templates (confirmation d'inscription et réinitialisation du mot de passe), variable `{{ .ConfirmationURL }}`. Le texte des modèles est en français, aux couleurs de l'app (marine `#17294e`, bleu `#2a5bea`). Supabase interdit de modifier les modèles sans SMTP personnalisé : Nolan a branché le SMTP de Gmail (`smtp.gmail.com`, port 465, adresse `the.analytics.app@gmail.com`, mot de passe d'application Google, jamais partagé avec Claude), limite 30 e-mails par heure côté Supabase, Gmail limité à environ 500 par jour. Modèles collés et validés par Nolan le 2026-09-25. Étape suivante possible plus tard : nom de domaine et Resend, pour un expéditeur « The Analytics » et sans limite Gmail (Supabase avertit que Gmail est prévu pour du courrier personnel, les e-mails peuvent finir en indésirables).
- **Page d'analyse** : en-tête (`buildCompanyMeta`, `buildCompanyKpis`, rafraîchis par `refreshCompanySummary` sans toucher au champ du nom), navigation collante par section (`buildSectionNav(c)`), chaque section a un `id="sec-<clé>"`, un sous-titre (`SECTION_SUBTITLES`) et un indicateur de remplissage (`progressHtml`, rafraîchi par `refreshSectionProgress`). Chaque bouton de `buildSectionNav` reçoit la couleur de sa section en inline `style="--sec-c:..."` (celles de `SECTION_COLORS`, le blanc chaud `#f3e9d7` pour Synthèse globale) : la pastille numérotée (`.n`) et, une fois actif, un fond légèrement teinté (`color-mix` avec `var(--card)`) + une bordure pleine dans cette couleur reprennent `var(--sec-c)`, le texte restant toujours `var(--ink)` (jamais blanc sur fond clair, pour éviter tout souci de contraste avec le blanc chaud ou un gris clair). Le bouton "Position" n'a pas de section propre : sa pastille (`positionNavColor(c)`, un point plutôt qu'un chiffre) est verte (`var(--green)`) ou rouge (`var(--red)`) selon le signe de la plus/moins-value calculée par `positionMetrics(c)`, et gris clair (`var(--border-strong)`) si aucune position active ou si le gain n'est pas calculable. Elle est recalculée en direct par `refreshSectionNavPosition()` (appelée dans `refreshDerived()`), sans attendre un rechargement de la page.
- **Identité par section (1 à 5)** : `SECTION_COLORS` (clé → couleur unique) est posée en variable CSS inline `style="--sec-c:..."` sur chaque `.section` ; toute la déclinaison visuelle de cette section (bordure du haut, cercle numéroté, fond légèrement teinté du bandeau de titre, barre de progression) lit `var(--sec-c)` par cascade, sans classe dédiée par section. Couleurs (validées avec Nolan via un aperçu de plusieurs teintes le 2026-09-23) : Identité (1) bleu `#2a5bea`, Financier (2) indigo `#4338ca`, Croissance (3) cyan électrique `#06b6d4` (choisi pour un rendu "futuriste"), Valorisation (4) or `#c9a24a`, Décision (5) fuchsia `#a3227a`. **Aucune section ne doit être verte, rouge, sarcelle (trop proche du vert) ni bordeaux (trop proche du rouge)** : ces teintes sont réservées au barème (`TIER_COLORS`) pour ne jamais laisser croire à un signal "bon/mauvais" sur une section entière — vérifier ce point avant toute nouvelle demande de "changer les couleurs" plutôt que de deviner. La section 6 (Synthèse globale, `buildInsightsSection`) n'a pas d'entrée dans `SECTION_COLORS` mais sa propre couleur d'identité posée en inline sur `.insights-section` : `--sec-c:#f3e9d7`, un blanc chaud (demande de Nolan le 2026-09-23), repris par la bordure du haut, le fond du bandeau titre (`color-mix` avec le navy) et le cercle numéroté (texte navy pour le contraste). Le sélecteur `.insights-section{border-color:var(--border-strong); border-top-color:var(--sec-c)}` doit garder les deux déclarations dans cet ordre : le raccourci `border-color` pose les 4 côtés puis `border-top-color` réaffirme spécifiquement le haut, sinon le raccourci écrase la couleur de section.
- **À savoir** : les textes déjà existants dans l'app (infobulles, synthèse) sont encore au tutoiement, ceux de l'accueil au vouvoiement. Harmoniser si l'app est ouverte à des utilisateurs tiers.

## Politique de dividende (demande de Nolan le 2026-09-24)
`identite.politiqueDividende` n'est plus un texte riche mais un objet `{ mode, dividendeAction, frequence, payout, croissance, annees, notes }` (type `dividend`, retiré de `RICH_FIELDS`). `mode` vaut `'aucun'` ("Ne verse pas de dividende"), `'verse'` ("Verse des dividendes") ou `''` (pas encore choisi ; recliquer sur l'option active la désélectionne). Quand `mode === 'verse'`, des champs apparaissent : dividende annuel par action (devise de l'analyse), rendement du dividende (CALCULÉ, `computeDividendYield` = dividende ÷ prix actuel, case bleue, rafraîchie par `refreshAllComputed`), fréquence (liste), taux de distribution (CALCULÉ aussi, `computeDividendPayout` = dividende par action ÷ EPS actuel de la section Valorisation, "—" si l'EPS manque ou est négatif ; la clé `payout` saisie autrefois n'est plus lue), croissance du dividende sur 5 ans (%), **sécurité du dividende** (note saisie de 0 à 100, clé `securite`, remplace les "années de hausse consécutives" à la demande de Nolan ; barème propre à ce champ et NON dans `SCALE_CONFIG` : 0-25 très dangereux rouge, 25-50 dangereux orange `#f2994a`, 50-75 sûr vert pâle, 75-100 très sûr vert foncé, via `dividendSecurityTier`/`dividendSecurityBadge`/`dividendSecurityTip`, pastille `#div-secu-badge` rafraîchie par `refreshAllComputed`), précisions. Les sous-champs réutilisent les attributs `data-duo-*` (donc `updateDuo`, sans écouteur dédié) ; seul le choix du mode a le sien (`data-div-mode`, re-rend la page). La case "Précisions" (`notes`) peut être enlevée par un ✕ (`data-div-notes="hide"`, clé `notesHidden = '1'`, chaîne comme les autres sous-champs) et remise par le lien "+ Remettre la case Précisions" (`data-div-notes="show"`) : le texte saisi n'est JAMAIS effacé, seul l'affichage change, pour qu'une suppression par erreur soit réversible (demande de Nolan le 2026-09-25). Valable en mode "verse" comme en mode "aucun". Migration dans `migrateCompany` (donc aussi à l'import) : l'ancien texte est converti en texte simple dans `notes`, le mode reste vide pour que l'utilisateur choisisse ; toute valeur non texte est remise à `''`.

## Page Portefeuille (demande de Nolan le 2026-09-24)
Vue `currentView === 'portfolio'` (`renderPortfolioView`, `goPortfolio`, entrée "Portefeuille" dans la barre latérale, `#navPortfolio`), sortie de Mon espace (la carte `buildPortfolioCard` a été supprimée ; Mon espace garde seulement les tuiles "Capital investi" et "Résultat latent"). Classes CSS préfixées `pf-` plus les composants de Mon espace (`.space-head`, `.space-kpis`, `.dkpi`, `.sgrid`, `.scard`). Tout est calculé à partir de `positionMetrics`/`portfolioSummary` : jamais de conversion de devise, totaux et répartitions séparés par devise (pastilles "Dollars/Euros" `data-pf-cur` pour choisir la devise des graphiques). Contenu : 6 tuiles (capital investi, valeur actuelle, résultat latent, performance, nombre de positions en hausse/baisse, meilleure et plus faible position), tableau des positions (quantité, prix d'achat, cours, investi, valeur, résultat en montant et %, poids, décision, score, lignes de total par devise), performance par position (barres divergentes `pf-perf-*`), allocation en donut avec indicateurs de concentration neutres (première position, trois premières, nombre de secteurs et de pays, pastille "Bien réparti/Concentration modérée/Portefeuille concentré", jamais vert/rouge), répartition par secteur et par pays pondérée par le capital investi, "Points d'attention" (`pfAlerts` : position ≥ 40 % du capital, secteur ≥ 60 %, recul ≥ 15 %, hausse ≥ 50 %, surévaluation supérieure à 10 % selon la valeur intrinsèque (voir la formule d'écart ci-dessous), thèse absente, données de position incomplètes ; ce sont des rappels, jamais des conseils) et "Idées à suivre" (analyses sans position en Surveiller/Acheter avec distance au prix d'achat cible). État vide pédagogique quand aucune position n'est suivie. `refreshCurrentPrice` re-rend cette page (comme Mon espace) quand un prix se met à jour.

## Section Apprentissage (demande de Nolan le 2026-09-24)
Objectif : que n'importe qui, même sans aucune connaissance, puisse utiliser l'app et se former à la bourse. Vue `currentView === 'learn'` (`renderLearnView`, `goLearn(id)`), entrée "Apprentissage" dans la barre latérale (`#navLearn`), boutons `data-go-learn[="<id>"]` (accueil, carte "Le mot du jour" de Mon espace) gérés par un écouteur délégué. Classes CSS préfixées `lr-` (ne pas confondre avec `.learn*` de l'accueil).
- **Contenu 100 % statique**, écrit à la main dans le fichier : `LEARN_CHAPTERS` (10 chapitres : bases, prise en main, méthode value/qualité, remplir chaque section, dictionnaire des indicateurs, exemple pas à pas, pièges, glossaire, quiz, FAQ), `LEARN_GLOSSARY` (~66 termes), `LEARN_QUIZ` (11 QCM expliqués), `LEARN_FAQ`, `LEARN_INDICATORS` (complément par clé de champ : `how`, `ex`, `warn`), `LEARN_RULE_TEXT` (les 10 critères Buffett, dont ceux qui se cochent automatiquement). Les blocs se construisent avec les fabriques `LP/LH/LUL/LTIP/LDEFS/LTABLE/LFORM/LSTEPS` (+ `{t:'btn'}`, `{t:'buffett'}`). Vouvoiement, pas de tiret cadratin, aucun chiffre de marché inventé : les exemples chiffrés sont fictifs ("Alpha Tech", entreprise imaginaire) et le contenu rappelle que ce n'est pas un conseil en investissement. Les textes sont des gabarits JS (backticks) : ne pas y mettre de `${` ni de backtick.
- **Le dictionnaire des indicateurs est dérivé de `SECTIONS`** : il liste chaque champ ayant une entrée dans `FIELD_DESCRIPTIONS` ou `LEARN_INDICATORS`, avec son barème de couleur réel (`scaleTooltip`, dans le même habillage `.tip-scale` que "Le mot du jour"). Quand on ajoute un champ à `SECTIONS`, lui ajouter une description (`FIELD_DESCRIPTIONS`) et si possible une entrée `LEARN_INDICATORS` : il apparaît alors tout seul. Si on change un barème, les exemples chiffrés du chapitre "Exemple complet" (PEG 1,33 bon, ROIC 22 % très bon, marge nette 21 % très bonne, CAGR 12 % bon, conversion FCF 85 % bonne, dette/FCF 1,2 bon, MOAT 7 bon) doivent être revérifiés avec `getScaleTier`.
- **Ajustements demandés par Nolan (2026-09-24)** : les temps de lecture (`min` de chaque chapitre) sont volontairement courts, ceux d'origine étaient jugés 2 fois trop longs (total ~55 min) ; les sources d'information recommandées sont le site web de l'entreprise (rubrique Investisseurs), TradingView (tradingview.com) et Alpha Spread (alphaspread.com), versions gratuites "assez complètes" selon Nolan, liens en `target="_blank" rel="noopener noreferrer"` dans le chapitre "Remplir chaque section", la FAQ et l'étape 3 de la prise en main. La page Apprentissage utilise toute la largeur (`.wrap.page.lr-wrap`, max 1680px, texte courant jusqu'à 1040px) et les grilles de définitions/glossaire se répartissent en 2 ou 3 colonnes (`.lr-defs-2` quand le nombre d'éléments n'est pas multiple de 3, pour ne pas laisser de trou).
- Progression : `state.settings.learnRead` (liste d'identifiants de chapitres lus), bouton "Marquer comme lu" (`paintLearnProgress`). Recherche instantanée (sans accents, `learnNorm`) dans le dictionnaire (avec filtre par section) et le glossaire, quiz interactif (`learnQuizAnswers`, non sauvegardé), FAQ en `<details>`.

## Graphique des prévisions Wall Street
Sous le champ `wallStreet` (Max/Moy/Min), `buildWallStreetChart(c)` dessine un graphique "en éventail" sur fond marine (carte `.ws-chart`) qui ressemble à un vrai graphique de prévisions boursières :
- **Saisie (changée le 2026-09-25 à la demande de Nolan)** : l'utilisateur saisit désormais les PRIX CIBLES à 1 an (Max, Moy, Min, en devise de l'analyse, avec le symbole automatique) et non plus les pourcentages. `computeWallStreetTargets(c)` lit ces prix (`parseMoney`) et calcule le % = `(prix cible ÷ prix actuel) − 1`, affiché automatiquement à côté de chaque case (`#ws-pct-max|moy|min`, case bleue calculée avec infobulle, plus une pastille de couleur `#ws-badge-*`, mises à jour par `refreshWsPercents`, appelée par `refreshWallStreetChart`). Barème (`wallStreetTier`/`wallStreetColor`/`wsScaleTooltip`) : négatif rouge (mauvais), 0 à moins de 10 % jaune (moyen), 10 à moins de 30 % vert clair (bon), 30 % et plus vert foncé (très bon) ; il colore aussi les rayons et bandes du graphique, qui lui n'a pas changé. Le résumé de valorisation ("Wall St. moy") et la phrase de synthèse utilisent le % calculé. **Migration** : avant, les cases contenaient des pourcentages. `wallStreet.unit === 'price'` marque le nouveau format ; `applyLegacyWallStreet(c)` (appelée par `migrateCompany`, au début de `render()` d'une fiche et après une mise à jour automatique du prix) convertit les anciens % en prix (`prix actuel × (1 + %/100)`, arrondi au centime) ; sans prix actuel, les anciens % sont gardés dans `wallStreet.legacyPct` (affichés dans une note sous les cases) puis convertis dès que le prix existe, jamais perdus. `updateWallStreetField` et `newCompany` posent toujours `unit:'price'`, sinon un prix saisi serait pris pour un ancien % au rechargement.
- **Jauge "Consensus des analystes" (2026-09-25)** : sous les cases Max/Moy/Min et au-dessus du graphique, carte marine `.ws-reco` (`buildWsGauge`, `paintWsGauge`, `setWsReco`, `bindWsGauge`) : demi-cercle de 5 zones Strong Sell, Sell, Hold, Buy, Strong Buy (titres en anglais, choix de Nolan ; couleurs `WS_RECO` : rouge, jaune, gris, vert clair, vert foncé, demande de Nolan le 2026-09-26 ; `color` = teinte vive sur la carte marine, `tier` = palier `TIER_COLORS` utilisé pour le texte sur fond clair dans le résumé de valorisation, via `wsRecoTextColor`) et une aiguille. Réglage MANUEL : clic, glissement (pointer events, `touch-action:none`) ou clavier (flèches, Maj pour 0,5, Home/End, Suppr) ; bouton "Effacer". Stockée dans `wallStreet.reco`, chaîne de 1 à 5 avec une décimale (1 = Strong Sell, 5 = Strong Buy), 5 zones égales de 0,8 point (`wsRecoIndex`, avec une marge 1e-9 contre l'imprécision flottante : Hold est centré sur 3). Vide : aiguille estompée et "Non renseigné". Enregistrée seulement au relâchement (pas à chaque mouvement). La phrase de synthèse la cite. `isFieldFilled` pour `wallStreetTrio` ne regarde que `max/moy/min/reco` (`unit` et `legacyPct` ne comptent pas). Le texte de la jauge est en HTML sous le SVG, pas dans le SVG.
- Quatre rayons partent tous du même point de départ (prix actuel, aujourd'hui) et rejoignent leur cible en ligne droite à +1 an : "Actuel" est plein et bleu (`#5b8cf7`), Max/Moy/Min sont pointillés et colorés avec le même `wallStreetColor()` que les champs eux-mêmes (rouge / jaune / vert clair / vert foncé selon le barème ci-dessus).
- Des bandes triangulaires transparentes remplissent l'espace entre rayons consécutifs (triés par prix cible), couleur de la borne haute (une bande qui redescend vers "Actuel" depuis une valeur négative reste rouge).
- Axe de prix à l'échelle (`niceAxisTicks`, graduations rondes 1/2/5×10ⁿ) avec grille de fond.
- **Important : les étiquettes (catégorie + % + prix cible, graduations de l'axe) sont des `<div>` HTML positionnés en `top`/`left` en pourcentage par-dessus le SVG (`preserveAspectRatio="none"`), PAS du texte SVG.** Un texte SVG hérite du facteur d'échelle du `viewBox`, donc devient illisible dès que la carte est étroite (mobile) ; le texte HTML garde toujours sa taille de police fixe (13-13,5px), quelle que soit la largeur du graphique. Ne jamais revenir à du texte inline dans le SVG pour ce graphique.
- `.ws-chart-inner` a un `min-height:320px` (en plus de `aspect-ratio`) pour garantir assez de place verticale aux étiquettes même sur un écran très étroit ; en dessous de 640px de large, les graduations de l'axe (`.ws-axis-tick`) sont masquées (redondantes avec les étiquettes) pour éviter tout chevauchement horizontal.
- Les étiquettes sont recalées verticalement (`labelYByKey`, calculé en triant par position à l'écran, PAS par prix — un prix plus élevé se dessine plus haut) pour ne jamais se chevaucher quand deux cibles sont proches.
- `refreshWallStreetChart()` (id `wsChartWrap`) est appelé après chaque saisie sur prix actuel ou max/moy/min.
- Sans prix actuel, ou avec un prix actuel mais aucune des 3 estimations, un encart `.ws-chart-empty` invite à les renseigner.

## Mon espace, logos, préférences et sauvegarde
- **"Toutes vos analyses" (`buildCompaniesTable`)** : table volontairement compacte (classes `.home-table--tight`/`.home-table-wrap--tight`, scopées à cette seule table, ne pas les appliquer à celle du Portefeuille) pour tenir sans barre de défilement horizontale sur un écran normal (~1220px de large et plus) : en-têtes courts avec `data-tooltip` pour le mot complet, nom d'entreprise tronqué en ellipse, dates au format court (`formatDateFrShort`, jj/mm/aa). PEG, MOAT et Buffett reprennent le même code couleur par palier que partout ailleurs dans l'app (badges `TIER_COLORS` via `getScaleTier`/`scaleTier`, fonction `chip()` locale) ; la conviction s'affiche en petites étoiles dorées (`starsRowHtml(..., 'ct-stars')`, même composant que la barre latérale) plutôt qu'en texte. En dessous de ~1220px, un défilement horizontal réapparaît dans le cadre de la table (pas sur toute la page) : c'est le filet de sécurité voulu, pas un bug.
- **Mon espace** (`currentView === 'space'`, `renderSpaceView`, `goSpace`) : page personnalisée séparée de l'accueil, entrée "Mon espace" dans la barre latérale. Contenu : salutation (prénom en préférence), indicateurs, recommandations (depuis le 2026-09-25 : seulement 3, les analyses les plus anciennes par date de création qui ne sont pas encore terminées, c'est-à-dire fiche complétée à moins de 100 % ; `unfinishedAnalyses`, `buildRecsCard`, `REC_MAX` ; l'ancienne liste complète `buildRecommendations` est conservée mais n'est plus affichée, les points liés aux positions sont dans le Portefeuille), reprise de la dernière analyse, idées par décision, classement par score, cours vs valeur intrinsèque, objectifs d'achat, répartition secteur/pays, portefeuille (`positionMetrics`, `portfolioSummary`, totaux séparés par devise, jamais de conversion), tableau de toutes les analyses, progression (`buildMilestones`), "mot du jour" (`FIELD_DESCRIPTIONS` + barème) et préférences. Tout est calculé à partir des saisies de l'utilisateur : ne jamais y inventer de donnée de marché. Les recommandations sont des rappels et pistes de réflexion, jamais des conseils d'achat ou de vente (mention affichée dans la carte).
- **Logos automatiques** : `findLogo(name, ticker)` interroge Wikidata par NOM (entreprise reconnue, score de candidats, icône P8972, sinon favicon du site officiel P856, sinon logo P154 via Wikimedia Commons). Le ticker ne sert qu'à départager des homonymes : ne JAMAIS chercher un logo par ticker seul (MC ou AIR désignent d'autres sociétés aux États-Unis, ça donne de faux logos). Le logo est stocké dans `c.logo = { url, src:'auto'|'file', q }` ; `src:'file'` (image choisie ou logo retiré) n'est jamais écrasé par la recherche automatique. `avatarInner(c)` fournit l'avatar (logo ou initiales) partout ; les cases de logo (`.has-logo`) ont un fond blanc mais seulement 1 à 2px de marge intérieure pour que le logo remplisse la case (demande de Nolan le 2026-09-23 : "zone blanche trop grosse", logos agrandis), et sont plus grandes qu'avant (barre latérale 38px, en-tête d'analyse 72px) ; le fond blanc est gardé exprès car beaucoup de logos sont noirs ou transparents et disparaîtraient sur le marine ; une erreur de chargement retombe sur les initiales (`logoFailed`). Un échec réseau n'est pas mémorisé (nouvel essai au prochain chargement), un "aucun résultat" l'est (clé `q` = nom|ticker). Seul le nom de l'entreprise est envoyé aux services tiers, ce qui est indiqué dans les préférences (option `state.settings.autoLogos`).
- **Préférences** : `state.settings = { autoLogos, firstName, defaultCurrency, twelveDataApiKey }` (ajouté à côté de `companies`, sans migration nécessaire).
- **Prix actuel automatique (Twelve Data)** : demande explicite de Nolan le 2026-09-23 pour ne plus avoir à ressaisir le cours à la main chaque jour (positions ouvertes toujours à jour). L'app n'a pas de serveur : elle interroge directement Twelve Data depuis le navigateur avec la clé API personnelle de l'utilisateur, collée une fois dans Préférences (`state.settings.twelveDataApiKey`, champ `#prefPriceKey`).
  - **Un ticker seul est souvent ambigu entre plusieurs bourses** (ex. "MC" = LVMH à Paris en euros ET Moelis & Company à New York en dollars ; découvert le 2026-09-23 quand Nolan a testé LVMH en euros et récupérait le mauvais cours). `resolveTwelveDataMicCode(ticker, currencyCode, apiKey)` interroge d'abord `/symbol_search`, filtre les résultats dont le `symbol` correspond exactement au ticker, et prend celui dont la `currency` correspond à la devise de l'analyse (`CURRENCY_TO_TWELVE_DATA_CODE` : `$→USD`, `€→EUR`) pour en extraire le `mic_code` de la bonne bourse, transmis ensuite à `/quote?...&mic_code=...`. Résultat mis en cache par analyse (`c.priceMicCode` / `c.priceMicCodeFor`, clé `TICKER|DEVISE`) pour n'appeler `/symbol_search` qu'une fois par ticker/devise (les actualisations suivantes n'utilisent que `/quote`, 1 crédit au lieu de 2). Le cache est invalidé automatiquement dès que le ticker ou la devise de l'analyse change (la clé de cache change).
  - **Toujours pas de conversion de devise** (règle générale de l'app) : `TWELVE_DATA_CURRENCY_MAP` (`USD→$`, `EUR→€`) compare la devise reçue à `c.currency`. Si aucune cotation dans la bonne devise n'existe vraiment pour ce ticker chez Twelve Data (cas réel, pas une histoire d'homonyme), `c.priceError` explique la situation et conseille le dollar plutôt que d'écraser `prixActuel` ou d'inventer un taux de change.
  - **Le compte gratuit de Twelve Data ne couvre pas toutes les bourses** : découvert le 2026-09-23 en testant LVMH avec une vraie clé (message reçu : *"This symbol is available starting with the Grow or Venture plan"*). Même en trouvant le bon `mic_code` (Euronext Paris), l'appel `/quote` est refusé pour les bourses hors offre gratuite (notamment les bourses européennes) : ce n'est PAS un bug côté app, c'est une limite du plan gratuit de Twelve Data lui-même (les actions américaines NASDAQ/NYSE fonctionnent bien avec le compte gratuit). `refreshCurrentPrice` détecte ce cas (message d'erreur contenant `plan`/`upgrad`) et affiche un message clair conseillant le dollar au lieu du message brut en anglais de Twelve Data. Ne pas essayer de contourner cette limite techniquement (pas de proxy, pas d'autre astuce) : c'est un choix commercial du fournisseur, à respecter.
  - **Champ "Cotation" (Identité, `fields.identite.cotation`, liste déroulante)** : type `exchange`, `buildExchangePicker`) : l'utilisateur choisit sa place boursière parmi `EXCHANGES` (`label`, `country`, `mic`, `cur`) : NASDAQ, NYSE, Euronext Paris/Amsterdam, Xetra, Borsa Italiana, Bolsa de Madrid, affichés avec le drapeau du pays (`flagIcon`, SVG existants, pas d'emoji : ils ne s'affichent pas sous Windows) et sans nom de pays. Le sélecteur réutilise l'habillage `.country-picker*` ; ses options sont liées à part (`.exchange-list`), et le sélecteur de pays exclut `.exchange-list`. Pas d'option "Automatique" : tant qu'aucune bourse n'est choisie (placeholder "Choisir une bourse…"), l'app cherche la bourse selon la devise via `/symbol_search`, comme avant. `findExchange` accepte aussi l'ancien format enregistré (« NASDAQ (États-Unis) »). Si une bourse est choisie, son `mic` est utilisé directement (pas de `/symbol_search`, pas de cache `priceMicCode`). Si sa devise `cur` diffère de `c.currency`, `refreshCurrentPrice` affiche un message sans appeler le réseau. Un changement de cotation relance l'actualisation (`updateField`). Le type `select` accepte `emptyLabel` pour nommer l'option vide. Le dollar reste privilégié partout (le plan gratuit ne couvre que NASDAQ/NYSE) : message sur l'accueil (`#prices`), dans la modale "Nouvelle analyse" et dans les Préférences.
  - **Guide pas à pas** (`#priceGuideModal`, `openPriceGuide`/`savePriceGuideKey`, ouvert par tout élément portant `data-open-price-guide` via un écouteur délégué posé par `bindPriceGuide`) : 4 étapes (compte Basic gratuit, e-mail, tableau de bord, coller la clé) puis test réel de la clé (`/price?symbol=AAPL`) avant enregistrement. Points d'entrée : accueil, Préférences, lien "Activer le prix automatique" sous "Prix actuel" (masqué dès qu'une clé existe).
  - `refreshCurrentPrice(id, force)` : lit le ticker (`c.fields.identite.ticker`) et la clé, respecte un délai minimal de 5 minutes entre deux appels non forcés (`PRICE_REFRESH_MIN_INTERVAL_MS`, pour rester large sous la limite gratuite de Twelve Data), et un verrou `priceInFlight` par analyse pour éviter les doublons. Déclenché automatiquement à l'ouverture d'une fiche (`selectCompany`), après saisie du ticker (`updateField`, via `schedulePriceRefresh` comme `scheduleLogo`), après changement de devise (`setCompanyCurrency`, forcé), et au chargement de l'app pour toutes les analyses avec une position ouverte (`ensurePrices()`, pour que le portefeuille de Mon espace reste à jour sans avoir à ouvrir chaque fiche) ; `paintPriceStatus` met à jour l'input et le texte de statut ciblés sans re-rendre toute la page (et sans écraser un champ en cours de saisie), avec repli sur `renderSpaceView()` si Mon espace est affiché. Il n'y a plus de bouton "Actualiser" (retiré à la demande de Nolan le 2026-09-23) : le prix se met à jour tout seul aux moments listés ci-dessus. Le prix reçu est arrondi à 1 chiffre après la virgule (`Math.round(prix * 10) / 10`, demande de Nolan) avant d'être écrit dans `prixActuel`. Seul le ticker est envoyé à Twelve Data, jamais le contenu de l'analyse.
- **Sauvegarde automatique dans un fichier (étape 1, demande de Nolan le 2026-09-24)** : contexte, l'app reste locale (localStorage) et doit évoluer plus tard vers des comptes en ligne (Supabase ou Firebase, étape 2 non commencée, ajouter une politique de confidentialité avant l'ouverture au public ; approche prévue : local d'abord, copie en ligne ensuite). Module juste après `touchUpdated` : `fileBackup` (état), `chooseBackupFile` (`showSaveFilePicker`, Chrome/Edge ordinateur seulement, `fileBackup.supported`), le handle du fichier est mémorisé en IndexedDB (`the-analytics-backup`), `writeFileBackup` (appelé 2,5 s après chaque `saveState` via `scheduleFileBackup`, ou tout de suite quand l'onglet passe en arrière-plan), `reauthBackupFile` (après un redémarrage le navigateur peut redemander l'autorisation : bouton "Réactiver", nécessite un clic), `stopBackupFile`. On n'écrit JAMAIS un état vide (aucune analyse) par-dessus le fichier. `buildBackupPayload()` est la source unique du contenu (export manuel ET fichier automatique) et retire `twelveDataApiKey`. Le panneau `#backupPanel` (Préférences de Mon espace) est repeint par `paintBackupPanel`, donc ses boutons passent par des écouteurs délégués (`data-backup-action`, `#prefExport`, `#prefImport`), ne pas les lier dans `bindSpace`. Rappel : `buildBackupReminder()` (bandeau en haut de Mon espace) s'affiche s'il y a des analyses, pas de sauvegarde automatique active et aucune sauvegarde depuis 7 jours ; "Plus tard" le masque 3 jours. `state.settings.lastBackupAt`/`backupSnoozeUntil` en sont les compteurs.
- **Comptes en ligne (étape 2, demande de Nolan le 2026-09-24)** : Supabase, SANS bibliothèque (simples `fetch`, `sbFetch`). Projet `https://coygikeasyfiwuskfbqh.supabase.co`, clé PUBLIQUE `sb_publishable_...` en dur dans le code (`SUPABASE_URL`/`SUPABASE_KEY`), c'est voulu : la sécurité vient des règles RLS de la table `user_data` (`user_id uuid PK`, `data jsonb`, `updated_at`, une ligne par utilisateur, politiques select/insert/update/delete `auth.uid() = user_id`). Ne JAMAIS mettre la clé secrète/service_role dans le code. Module après le module de sauvegarde fichier : `cloud` (session dans `localStorage` clé `analytics-session`, séparée de `state` donc jamais exportée), `cloudSignUp/SignIn/Logout`, `cloudToken` (rafraîchit le jeton), `syncNow` (tire la ligne distante, `mergeRemote`, puis pousse `buildBackupPayload()` si le serveur n'a pas tout), `scheduleCloudSync` (4 s après chaque `saveState`), reprise au retour sur l'onglet, au retour du réseau et toutes les 2 min. Local d'abord : l'app marche sans compte et hors ligne. Fusion par analyse, la plus récente gagne (`companyStamp` = `updatedTs` sinon `updatedAt`). Les suppressions passent par des pierres tombales `state.deleted = { id: horodatage }` (posées par `confirmDeleteCompany`, retirées par l'import), sans elles une analyse supprimée reviendrait depuis un autre appareil. `OWNER_KEY` mémorise le compte propriétaire des données locales : si un autre compte se connecte, confirmation puis effacement local pour ne jamais mélanger deux comptes. Confirmation par e-mail activée côté Supabase (`mailer_autoconfirm:false`, l'option n'est pas visible dans leur tableau de bord) : l'app gère les deux cas. Interface : bouton `#accountBtn` en bas de la barre latérale, modale `#authModal` (`openAuth`, `paintAuthModal`, `submitAuth`, `bindAuth`), bloc "Compte en ligne" dans le panneau de sauvegarde, `backupProtected()` compte aussi un compte synchronisé. Testé avec un faux serveur en mémoire (jamais de vrai compte créé par Claude). Reste à faire : e-mails personnalisés en français (modèles Supabase puis SMTP type Resend avec nom de domaine), accueil pour les visiteurs, relecture juridique des textes.
- **Mot de passe oublié, suppression de compte, textes légaux (2026-09-25)** : modes de `#authModal` : `login`, `signup` (case de consentement obligatoire `#authConsent`, `consent_version` et `consent_at` envoyés dans `user_metadata` à l'inscription), `forgot` (`cloudRecover`, message identique que le compte existe ou non), `newpw` (`cloudSetPassword`, ouvert par `handleAuthRedirect` quand le lien de l'e-mail porte `type=recovery`, après `checkCloudOwner`), `account` (boutons "Télécharger mes données" = `exportData`, "Supprimer mon compte"), `delete` (taper SUPPRIMER, `deleteCloudAccount`). La suppression appelle la fonction SQL `public.delete_my_account()` via `/rest/v1/rpc/delete_my_account` (SECURITY DEFINER, supprime `auth.users` de l'appelant ; `user_data` part en cascade) ; si la fonction n'existe pas (404) l'app efface au moins la ligne `user_data` et le dit. SQL à exécuter une fois dans Supabase (SQL Editor) : `create or replace function public.delete_my_account() returns void language plpgsql security definer set search_path = public, auth as $$ begin delete from auth.users where id = auth.uid(); end; $$; revoke all on function public.delete_my_account() from public, anon; grant execute on function public.delete_my_account() to authenticated;`. Après suppression, les analyses locales restent sur l'appareil, `OWNER_KEY` est retiré. Vue `currentView === 'legal'` (`renderLegalView`, `goLegal(tab)`, onglets `LEGAL_TABS` : confidentialité, conditions, mentions légales, contenu `LEGAL_CONTENT` avec les fabriques `LP/LH/LUL`), liens dans le pied de la barre latérale (`.side-legal`) et via tout élément `[data-legal="<onglet>"]` (écouteur délégué dans `bindAuth`). Constantes à tenir à jour : `LEGAL_VERSION`/`LEGAL_DATE_TXT` (changer quand un texte change), `LEGAL_PUBLISHER` (nom de l'éditeur), `LEGAL_CONTACT` (adresse dédiée the.analytics.app@gmail.com, choisie par Nolan le 2026-09-25 ; serveurs Supabase en Europe, mentionné dans les textes). Mot de passe oublié testé en vrai par Nolan et validé ; suppression de compte à tester par Nolan avec un compte de test. Textes rédigés comme base de travail, non validés par un juriste : les faire relire avant une ouverture large. La classe `.legal` existe déjà (avertissement de l'accueil, centré) : ne pas la réutiliser (la page utilise `.legal-page`).
- **Dividendes du portefeuille, courbe dans le temps et visite guidée (2026-09-25)** :
  - **Rendement sur prix d'achat** : `positionMetrics(c)` renvoie aussi `dps` (dividende annuel par action, seulement si `politiqueDividende.mode === 'verse'`), `divAnnual` (dps × quantité) et `yoc` (dps ÷ prix d'achat en %). Bloc `#position-div` sous les champs de Position (`positionDividendHtml`, `refreshPositionDividend`, cases calculées `#computed-position-yoc` et `#computed-position-divincome` avec infobulles `tipYieldOnCost`/`tipDividendIncome`), rafraîchi par `updatePositionField` et `refreshAllComputed`. Sans dividende renseigné, une phrase invite à le faire (Identité) ; en mode "ne verse pas", rien ne s'affiche. `portfolioSummary().byDev[devise]` a en plus `div`, `nDiv`, `divYocNum`/`divBase` (rendement sur prix d'achat global), `divCurNum`/`divValue` (rendement sur cours actuel global). Carte "Dividendes attendus" (`pfDividends`) sur la page Portefeuille, par devise, avec la liste par position ; ce sont des estimations (dividende actuel, avant impôts), le texte le dit.
  - **Historique du portefeuille** : `state.history = { 'AAAA-MM-JJ': { t, c: { '$': { i: capital investi, v: valeur }, '€': {...} } } }`, une photo par jour, séparée par devise (jamais de conversion), `i` = capital des seules positions dont la valeur est connue (valeur moins résultat). `recordPortfolioSnapshot` (aucune écriture si rien n'a changé ; garde 1500 jours) est appelée à l'ouverture de la page Portefeuille et via `scheduleSnapshot` (au chargement et après chaque mise à jour de prix) : la courbe se construit donc seulement les jours où l'app est ouverte, et le dernier relevé du jour l'emporte. Courbe `pfHistory` (SVG en `preserveAspectRatio="none"` + `vector-effect:non-scaling-stroke`, graduations et dates en HTML par-dessus, jamais de texte dans le SVG, comme le graphique Wall Street), périodes 1 mois / 3 mois / 1 an / Tout (`portfolioRange`, `PF_RANGES`, la durée de "Tout" doit rester finie : 36500 jours, une valeur énorme rend la date invalide), infobulle au survol/toucher (`bindHistoryHover`). Moins de 2 points : message pédagogique. `history` fait partie de `buildBackupPayload()` (export, fichier automatique, synchro) et se fusionne jour par jour, le relevé le plus récent gagne (`mergeHistory`, qui assainit tout ce qui vient de l'extérieur avec `cleanHistoryEntry` ; appelée par `mergeRemote` et par l'import). Un saut de courbe peut venir d'un achat ou d'une vente : la note sous le graphique l'explique.
  - **Visite guidée** : `TOUR_STEPS` (5 étapes : accueil centré, Nouvelle analyse, Mon espace et Portefeuille, Apprentissage, compte), `startTour`/`showTourStep`/`placeTour`/`endTour`/`initTour`. Projecteur `.tour-hi` (ombre géante qui assombrit le reste) et bulle `.tour-pop` (à droite de l'élément sur ordinateur, en feuille en haut ou en bas sur mobile, où le menu latéral est ouvert le temps de la visite, mesure après 340 ms). Se lance seule au premier chargement (`state.settings.tourDone` faux) sur l'accueil ; `tourDone` est posé à vrai d'office pour qui a déjà des analyses. Touches Échap, flèches, Entrée. Relançable via tout élément `[data-start-tour]` (Mon espace, Préférences, bloc "Visite guidée"). Si on change le menu latéral, revérifier les sélecteurs de `TOUR_STEPS`.
- **Camemberts et anneaux : mise en évidence au survol (2026-09-25)** : chaque part (`buildPieSvgInner` pour les champs `pieChart` des analyses, `buildDonut` pour Mon espace et Portefeuille) est un élément `.pie-slice` avec `data-i` (rang dans la légende) et `data-tooltip` (libellé coloré, valeur saisie pour les camemberts, part du total). L'infobulle passe par le système habituel `initCustomTooltips` et est volontairement COMPACTE (demande de Nolan : pas de mention "Valeur saisie") : nom en couleur, puis la valeur saisie seule ; la part en % du total n'est ajoutée (`valeur · X %`) que si la valeur n'est pas déjà ce pourcentage (ex. "60%" s'affiche seule) ; pour les anneaux, uniquement la part en %. Dans le sens légende vers graphique, SEULE la petite pastille de couleur (`.pie-swatch`) déclenche la mise en évidence : survoler le texte de la légende ne fait rien (demande de Nolan). Le contenu de `data-tooltip` est ÉCHAPPÉ DEUX FOIS (le libellé est saisi par l'utilisateur, `escapeHtml(html contenant escapeHtml(libellé))`), ne pas le simplifier. `initPieHover` (écouteur délégué) pose `.pie-active` sur le conteneur (`.pie-wrap` ou `.dn-wrap`) et `.hl` sur la part et sur la ligne de légende correspondantes, dans les deux sens (survol de la légende allume la part) ; CSS : les autres parts à 45 % d'opacité, la part survolée grossit de 6 % avec une ombre (`transform-origin:90px 90px`, coordonnées du viewBox). Les transitions ne se jouent pas dans un onglet en arrière-plan : pour tester par script, les désactiver temporairement.
- **Sauvegarde manuelle** : export JSON (`exportData`) et import (`importDataFile`). Un fichier importé est traité comme une donnée NON fiable : `cleanImportedCompany` assainit les identifiants, dates, devises, images, logos et force le nettoyage des textes riches. Toute nouvelle donnée affichée en HTML doit passer par `escapeHtml` ou être validée à l'import.
- **Service worker** : ne met en cache que le site et les polices Google. Les recherches de logos et les appels à Twelve Data ne passent pas par le cache (le fetch handler ne touche pas aux origines externes autres que les polices).

## Habitudes de travail à poursuivre
- **Toujours tester avant de livrer** : ce projet a eu un incident où une modification avait cassé l'affichage sans qu'on le détecte immédiatement (peur de perte de données). Depuis, chaque changement significatif est testé via simulation JSDOM (charger le HTML avec des données factices, simuler des clics/saisies, vérifier l'absence d'erreur JS) avant d'être livré à l'utilisateur. Continuer cette discipline.
- Vérifier l'équilibre des accolades CSS et la syntaxe JS (`node --check`) avant chaque livraison.
- L'utilisateur n'est pas développeur — expliquer simplement, éviter le jargon, proposer des choix clairs plutôt que des questions ouvertes techniques.
- Préférences de l'utilisateur : pas de tirets cadratins "—" dans le texte (remplacer par des virgules).

## Déploiement (en ligne depuis le 2026-09-25)
Site : https://nolancurzillat.github.io/the-analytics/ (GitHub Pages, dépôt public `nolancurzillat/the-analytics`, branche `main`, racine). Git n'est pas dans le PATH : utiliser celui de GitHub Desktop, `C:\Users\nolan\AppData\Local\GitHubDesktop\app-3.6.6\resources\app\git\cmd\git.exe` (le numéro de version `app-x.y.z` peut changer, vérifier). Identité des commits configurée en local dans le dépôt (`nolancurzillat` + adresse noreply GitHub, pour ne pas publier l'e-mail personnel). Flux de mise à jour : Claude modifie, teste, fait `git add -A` et `git commit` ; NOLAN clique sur "Push origin" dans GitHub Desktop (Claude ne pousse pas lui-même, c'est public, toujours demander). Toujours bumper `CACHE_NAME` dans `service-worker.js` à chaque changement de l'app. `.gitignore` exclut `the-analytics-sauvegarde*.json` (la sauvegarde automatique de Nolan, DONNÉES PERSONNELLES, elle est dans le dossier du projet), `*.zip`, `*.bat`, `.claude/`. Ne jamais retirer ces exclusions. Supabase : "Site URL" et "Redirect URLs" pointent vers l'adresse du site ; le lien de confirmation e-mail ramène dans l'app, `handleAuthRedirect` connecte automatiquement. Le manifest et le service worker utilisent des chemins relatifs (le site est dans un sous-dossier `/the-analytics/`) : ne jamais mettre de chemin absolu commençant par `/`. Les données d'un utilisateur ne sont jamais dans le dépôt (navigateur + Supabase), donc une mise à jour n'y touche pas : toute évolution de forme passe par `migrateCompany`.
